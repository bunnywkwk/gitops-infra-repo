apiVersion: v1
kind: Service
metadata:
  name: fastapi-service

  labels:
    app.kubernetes.io/name: fastapi-app
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    nodePort: 30081
    name: http
  selector:
    app.kubernetes.io/name: fastapi-app
